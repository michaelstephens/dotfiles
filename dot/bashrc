#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '
[ -r /home/michaels69/.byobu/prompt ] && . /home/michaels69/.byobu/prompt   #byobu-prompt#
